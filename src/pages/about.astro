---
import BaseHead from '../components/BaseHead.astro';
import ExperienceItem from '../components/ExperienceItem.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import SkillBadge from '../components/SkillBadge.astro';
import { experience } from '../data/experience';
import { categoryLabels, skills } from '../data/skills';
import { siteConfig } from '../data/siteConfig';

const skillsByCategory = skills.reduce(
	(acc, skill) => {
		if (!acc[skill.category]) acc[skill.category] = [];
		acc[skill.category].push(skill);
		return acc;
	},
	{} as Record<string, typeof skills>,
);
---

<!doctype html>
<html lang="en" class="scroll-smooth">
	<head>
		<BaseHead
			title="About"
			description="Senior Software Engineer with 12 years of C#/.NET/Azure enterprise experience. Currently building AI-integrated systems and writing about agentic AI for the .NET ecosystem."
		/>
	</head>
	<body class="antialiased flex flex-col min-h-screen" data-zone="light">
		<Header />
		<main class="flex-1 max-w-4xl mx-auto px-4 sm:px-6 py-12">

			<!-- Professional Summary -->
			<section class="mb-16">
				<div style="border-left:3px solid var(--light-text-2);padding-left:0.75rem;margin-bottom:0.75rem;"><p class="font-mono mb-0" style="font-size:0.75rem;letter-spacing:0.06em;text-transform:uppercase;color:var(--light-text-2);">&lt;About&gt;</p></div>
				<h1 class="font-bold mb-8" style="font-size:2rem;color:var(--light-text);letter-spacing:-0.03em;">Laurance Walden</h1>

				<div class="space-y-4 leading-relaxed max-w-2xl" style="color:var(--light-text-2);">
					<p>
						I'm a senior software engineer with over a decade of experience building enterprise-grade
						distributed systems in C#, .NET, and Azure. Most of that work lives in the invisible layer
						of large organizations — the APIs, pipelines, and services that quietly keep things running.
						I like it there. There's something satisfying about infrastructure that works so well nobody
						notices it.
					</p>
					<p>
						Across roles in retail, HR tech, and identity verification I've shipped production systems
						where reliability isn't optional. Highlights include cutting a multi-step workflow's processing
						time by 96% (6 minutes to 12 seconds), reducing daily failed transactions from 2,000 to under
						one, and automating away over 1,000 hours of annual manual labor. The pattern that keeps
						showing up: find the thing that's slow, fragile, or tedious, and make it not that.
					</p>
					<p>
						Right now my focus is the intersection of .NET and agentic AI. I'm building
						<a href={siteConfig.social.github + '/AIAgentMinder'} style="color:var(--light-text);text-decoration:underline;text-underline-offset:3px;">AIAgentMinder</a> —
						a governance framework for AI agents in enterprise .NET systems — and writing about what it
						actually takes to put AI into production. Not the happy-path demos, but the real questions:
						audit trails, lifecycle management, failure modes, and the organizational constraints nobody's
						talking about yet.
					</p>
					<p>
						I write here and on <a href={siteConfig.social.medium} style="color:var(--light-text);text-decoration:underline;text-underline-offset:3px;">Medium</a>.
						If you're working on .NET + AI systems or just want to compare notes on the space, find me on
						<a href={siteConfig.social.linkedin} style="color:var(--light-text);text-decoration:underline;text-underline-offset:3px;">LinkedIn</a>.
					</p>
				</div>
			</section>

			<!-- Experience -->
			<section class="mb-16">
				<h2 class="font-mono mb-8" style="font-size:0.8rem;letter-spacing:0.06em;text-transform:uppercase;color:var(--light-text-2);">&lt;Experience&gt;</h2>
				<div class="space-y-0">
					{experience.map((entry) => (
						<ExperienceItem
							company={entry.company}
							title={entry.title}
							startDate={entry.startDate}
							endDate={entry.endDate}
							description={entry.description}
							highlights={entry.highlights}
							technologies={entry.technologies}
						/>
					))}
				</div>
			</section>

			<!-- Skills -->
			<section class="mb-16">
				<h2 class="font-mono mb-8" style="font-size:0.8rem;letter-spacing:0.06em;text-transform:uppercase;color:var(--light-text-2);">&lt;Stack&gt;</h2>
				<div class="space-y-6">
					{(Object.entries(skillsByCategory) as [string, typeof skills][]).map(([category, categorySkills]) => (
						<div>
							<h3 class="font-mono mb-3" style="font-size:0.65rem;letter-spacing:0.06em;text-transform:uppercase;color:var(--light-text-2);">
								{categoryLabels[category as keyof typeof categoryLabels]}
							</h3>
							<div class="flex flex-wrap gap-2">
								{categorySkills.map((skill) => (
									<SkillBadge name={skill.name} proficiency={skill.proficiency} zone="light" />
								))}
							</div>
						</div>
					))}
				</div>
				<p class="font-mono mt-6" style="font-size:0.65rem;letter-spacing:0.04em;color:var(--light-text-2);">
					brightness = proficiency: black = expert · gray = advanced · dim = intermediate
				</p>
			</section>

			<!-- Certifications -->
			<section class="mb-16">
				<h2 class="font-mono mb-6" style="font-size:0.8rem;letter-spacing:0.06em;text-transform:uppercase;color:var(--light-text-2);">&lt;Certifications&gt;</h2>
				<ul class="space-y-2" style="color:var(--light-text-2);">
					<li class="text-sm">Certified Scrum Master (CSM) — Scrum Alliance, 2020</li>
				</ul>
				<p class="text-sm mt-4" style="color:var(--light-text-2);">
					Microsoft Azure certifications in progress for 2026.
				</p>
			</section>

		</main>
		<Footer />
	</body>
</html>
